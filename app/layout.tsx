import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "OQOOD",
  description:
    "OQOOD is a dynamic company specializing in contracting, trading, and manufacturing. We are committed to ethical practices, innovation, and timely delivery, serving the diverse needs of our clients in Saudi Arabia's industrial sector",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster richColors />
      </body>
    </html>
  );
}
